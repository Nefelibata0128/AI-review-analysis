{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/Dockerfile"
  },
  "deploy": {
    "startCommand": "python proxy-server.py",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
